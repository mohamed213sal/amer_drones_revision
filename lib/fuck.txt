import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:amerdrones/drones_pages/onboarding_drones_module.dart';

class OnBoardingDrones extends StatefulWidget {
  @override
  _OnBoardingDronesState createState() => _OnBoardingDronesState();
}

class _OnBoardingDronesState extends State<OnBoardingDrones> {
  //some properties for this class ya amer
  TextStyle style_one = TextStyle(
    color: Colors.white,
    fontWeight: FontWeight.bold,
    fontSize: 25,
  );

  TextStyle style_two = TextStyle(
    color: Colors.amber,
    fontWeight: FontWeight.normal,
    fontSize: 20,
  );

  List<OnBoardingDronesModule> GreatList = List<OnBoardingDronesModule>();

  //creating some methods for this class ua amer
  void GenerateList() {
    GreatList = [
      OnBoardingDronesModule(
          'First Page',
          'drones are very powerful tool for watching',
          'assets/images/bg10.png',
          Icons.build),
      OnBoardingDronesModule(
          'Second Page',
          'drones are very powerful tool for watching',
          'assets/images/bg11.png',
          Icons.add),
      OnBoardingDronesModule(
          'Third Page',
          'drones are very powerful tool for watching',
          'assets/images/bg12.png',
          Icons.widgets),
      OnBoardingDronesModule(
          'Fourth Page',
          'drones are very powerful tool for watching',
          'assets/images/bg13.png',
          Icons.add_a_photo),
    ];
  }

  @override
  Widget build(BuildContext context) {
    GenerateList() ;
    return Stack(
      children: <Widget>[
        PageView.builder(
          itemBuilder: (Buildcontext, index) {
            return Stack(
              children: <Widget>[
                Container(
                  decoration: BoxDecoration(
                    image: DecorationImage(
                      image: ExactAssetImage(GreatList[index].content),
                    ),
                  ),
                ),
                Column(
                  mainAxisAlignment: MainAxisAlignment.center,
                  crossAxisAlignment: CrossAxisAlignment.center,
                  children: <Widget>[
                    Transform.translate(
                      child: Icon(
                        GreatList[index].icon,
                        size: 24,
                        color: Colors.white70,
                      ),
                      offset: Offset(0,-50),
                    ),
                    Text(
                      GreatList[index].title,
                      style: style_one,
                    ),
                    Text(
                      GreatList[index].description,
                      style: style_two,
                    ),
                  ],
                ),
              ],
            );
          },
          itemCount: 1,
        ),
        Align(
          alignment: Alignment.bottomCenter,
          child: RaisedButton(
            child: SizedBox(
              height: 40,
              width: double.infinity,
              child: Text("GET STARTED"),
            ),
            onPressed: (){
              print("fuck you ya man") ;
            },
          ),
        ),
      ],
    );
  }
}
